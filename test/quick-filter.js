/**
 * WeWeb formula → bind to **Quick filter** on **TanStack Table (Guestwhat)**.
 *
 * Client-side: substring match over `JSON.stringify(row)` (case-insensitive).
 *
 * Examples:
 * - Smoke test: return 'julien'
 * - Variable (rename to your project): return variables?.userSearch ?? ''
 * - Disabled: return ''
 *
 * Return a string (or number). A bare binding object makes the filter substring `'[object Object]'` and hides every row.
 */

return '';
